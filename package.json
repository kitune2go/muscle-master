{
  "name": "muscle-master",
  "version": "1.5.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "check": "node --check app-core.js && node --check app.js && node --check sw.js && npm test"
  }
}
