# Rio Canonical Asset Import Status

Status: DRAFT / BINARY IMPORT PENDING

This branch starts from the latest `main` after PR #2.

Approved generated assets:
- Base Master
- Hero
- Portrait
- Level Up
- Expressions: 8
- Chibi: 5

The prior PR #3 is closed and will not be merged because it contains binary-storage experiments and was based on an older `main`.

The final runtime target is `assets/trainers/rio/`. Do not merge this branch until all approved WebP files and `manifest.json` are present and validation passes.
